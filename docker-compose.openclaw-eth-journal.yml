version: '3.8'
services:
  freqtrade-openclaw-eth-journal:
    image: freqtrade-local:postgres
    container_name: freqtrade-openclaw-eth-journal
    ports:
      - "8130:8130"
    volumes:
      - ./user_data:/freqtrade/user_data
    command: >
      trade
      --config /freqtrade/user_data/config_openclaw-eth-journal.json
      --strategy OpenClawBtcJournal
      --db-url sqlite:////freqtrade/user_data/trades-openclaw-eth-journal.sqlite
      --dry-run
    restart: unless-stopped
    stop_grace_period: 2m
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "3"
